---
- name: Debian - Add/Remove extra groups
  group:
    name: "{{ item.value.name }}"
    state: "{{ item.value.state }}"
    gid: "{{ item.value.gid }}"
  with_dict: "{{ aspects_local_users_groups }}"
  tags:
    - aspects_local_users
    - aspects_local_users_groups